import { Text } from 'react-native';
import Layout from '../../components/layout';

const ProfileScreen = () => {
  return (
    <Layout>
      <Text>Profile</Text>
    </Layout>
  );
};

export default ProfileScreen;
