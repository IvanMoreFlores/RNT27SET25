import { Text } from 'react-native';
import Layout from '../../components/layout';

const HomeScreen = () => {
  return (
    <Layout>
      <Text>Home</Text>
    </Layout>
  );
};

export default HomeScreen;
